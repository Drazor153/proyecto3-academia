export enum AnnTargets {
  ALL = 'ALL',
}

export enum EnrolsStatus {
  Active = 'active',
}

export enum RoleEnum {
  Student = 'STUDENT',
  Teacher = 'TEACHER',
  Admin = 'ADMIN',
}

export enum UserStatus {
  Enabled = 'ENABLED',
}
